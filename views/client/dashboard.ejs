<div class="max-w-7xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">

        <!-- Welcome Card -->
        <div
            class="md:col-span-2 bg-gradient-to-br from-indigo-900/50 to-slate-900 border border-slate-800 rounded-2xl p-8 relative overflow-hidden group">
            <div
                class="absolute top-0 right-0 -mt-10 -mr-10 w-48 h-48 bg-indigo-500/20 blur-3xl rounded-full group-hover:bg-indigo-500/30 transition duration-700">
            </div>
            <div class="relative z-10">
                <h2 class="text-3xl font-bold text-white mb-2">Xin chào, <%= user.full_name %>!</h2>
                <p class="text-slate-400 max-w-xl">Chào mừng bạn đến với hệ thống quản lý. Tại đây bạn có thể kiểm tra
                    số
                    dư, quản lý thông tin cá nhân và xem các dịch vụ đang hoạt động.</p>
            </div>
        </div>

        <!-- Balance Card -->
        <div
            class="bg-slate-900 border border-slate-800 rounded-2xl p-6 shadow-lg shadow-black/20 hover:border-slate-700 transition duration-300">
            <div class="flex justify-between items-start mb-4">
                <div>
                    <p class="text-sm font-bold text-slate-500 uppercase tracking-widest">Số dư hiện tại</p>
                    <h3 class="text-4xl font-bold text-emerald-400 mt-2 tracking-tight">
                        <%= new Intl.NumberFormat('vi-VN', { style: 'currency' , currency: 'VND' }).format(user.balance)
                            %>
                    </h3>
                </div>
                <div class="p-3 bg-emerald-500/10 rounded-xl text-emerald-400">
                    <i class="ri-wallet-3-fill text-2xl"></i>
                </div>
            </div>
            <div class="mt-4 pt-4 border-t border-slate-800 flex gap-3">
                <button disabled
                    class="flex-1 py-2 px-4 bg-slate-800 text-slate-500 rounded-xl text-sm font-bold cursor-not-allowed border border-slate-700/50 flex justify-center items-center gap-2">
                    <i class="ri-add-line"></i>
                    Nạp tiền
                </button>
                <button disabled
                    class="flex-1 py-2 px-4 bg-slate-800 text-slate-500 rounded-xl text-sm font-bold cursor-not-allowed border border-slate-700/50 flex justify-center items-center gap-2">
                    <i class="ri-vip-crown-line"></i>
                    Mua gói
                </button>
            </div>
            <p class="text-xs text-slate-500 mt-3 text-center italic">Tính năng nạp tiền và mua gói đang phát triển</p>
        </div>

        <!-- Quick Stats -->
        <div
            class="bg-slate-900 border border-slate-800 rounded-2xl p-6 shadow-lg shadow-black/20 hover:border-slate-700 transition duration-300">
            <h3 class="text-sm font-bold text-slate-500 uppercase tracking-widest mb-6">Thông tin tài khoản</h3>

            <div class="space-y-4">
                <div class="flex items-center justify-between p-3 bg-slate-950/50 rounded-xl border border-slate-800">
                    <div class="flex items-center gap-3">
                        <div class="p-2 bg-indigo-500/10 rounded-lg text-indigo-400">
                            <i class="ri-fingerprint-line text-xl"></i>
                        </div>
                        <span class="text-sm font-medium text-slate-300">Mã thành viên</span>
                    </div>
                    <span class="font-mono text-sm font-bold text-white bg-slate-800 px-2 py-1 rounded">#<%= user.id %>
                    </span>
                </div>

                <div class="flex items-center justify-between p-3 bg-slate-950/50 rounded-xl border border-slate-800">
                    <div class="flex items-center gap-3">
                        <div class="p-2 bg-indigo-500/10 rounded-lg text-indigo-400">
                            <i class="ri-calendar-line text-xl"></i>
                        </div>
                        <span class="text-sm font-medium text-slate-300">Gia nhập</span>
                    </div>
                    <span class="text-sm font-medium text-slate-400">
                        <%= new Date(user.createdAt).toLocaleDateString('vi-VN') %>
                    </span>
                </div>

                <div class="flex items-center justify-between p-3 bg-slate-950/50 rounded-xl border border-slate-800">
                    <div class="flex items-center gap-3">
                        <div class="p-2 bg-indigo-500/10 rounded-lg text-indigo-400">
                            <i class="ri-checkbox-circle-line text-xl"></i>
                        </div>
                        <span class="text-sm font-medium text-slate-300">Trạng thái</span>
                    </div>
                    <span
                        class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-bold bg-emerald-500/10 text-emerald-400 border border-emerald-500/20">Hoạt
                        động</span>
                </div>
            </div>
        </div>
    </div>
</div>